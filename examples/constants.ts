export enum BotEvents {
  AUTH = 'auth_failure',
  QR = 'qr',
  READY = 'ready',
  MESSAGE = 'message'
}

export enum Postman {
  WEB = 'web',
  CONTENT_TYPE = 'multipart/form-data'
}

export enum MessageType {
  TEXT = 'text'
}
